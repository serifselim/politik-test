import React from 'react'

const Question = ({text}) => {

    return (
        <div className="question">
            <span className="question-title">{text}</span>
        </div>
    )
}

export default Question;

